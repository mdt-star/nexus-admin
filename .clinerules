# Cline AI 运行规范 (v5.5-精简高控自适应版)

## 0. 🛑 核心红线：绝对隐私与去标识化 【规则_0】
- **【绝对匿名】** 严禁在任何文件、对话或记忆库中写入设备、用户或本地环境痕迹：
  1. **相对路径**：必须以项目根目录为基准（如 `./src/`），严禁物理绝对路径（如 `/Users/...`, `C:\Users\...`）。终端报错包含绝对路径时必须手动脱敏。
  2. **环境阻断**：严禁记录或输出本地 IP、MAC、OS 版本、Git 用户名/邮箱、明文密钥或凭证。
  3. **剔除签名**：严禁添加包含时间戳或 AI 标志的头部注释（如 `// @author Cline`）。
- **【业务注释】** 严禁以隐私审查为由减少注释。复杂逻辑、算法、API 必须编写高水准纯业务注释（遵守 JSDoc/PHPDoc/Doxygen），不含路径人名的注释均为安全。

## 1. 🔄 强控冷启动与格式死锁 【规则_1】
- **【首行死锁】** 任何全新会话（Session）收到首个任务时，**首行回复**必须强制输出以下状态标签，严禁在其前生成任何业务代码或解释：

  * **分支 A：若 `./.memory_bank/` 不存在**
    1. 首行必须且只能回复：`【当前状态：记忆银行未找到 - 正在初始化】`
    2. **【动作中断】** 立即挂起所有业务开发，严禁修改或创建任何业务文件。
    3. **【环境扫描】** 扫描根目录结构指纹（如 `package.json`, `pnpm-workspace.yaml`, `cargo.toml` 等），自主判定项目形态（单包/Monorepo/多应用）并分析技术栈。
    4. **【档案初始化】** 立即在 `./.memory_bank/` 下创建 6 个**纯英文文件名**文件，**内部内容必须使用纯中文**书写：`techContext.md`、`architecture.md`、`systemPatterns.md`、`activeContext.md`、`progress.md`、`publishing.md`（发布 SOP，初始化时根据判定形态写入对应模板）。
    5. 初始化完成后回复：`【当前状态：初始化成功】正在进入懒加载对齐流程...` 并立即切换至【规则_2】阶段 A。

  * **分支 B：若 `./.memory_bank/` 已存在**
    1. 首行必须且只能回复：`【当前状态：记忆银行已对齐】` 并立即进入 【规则_2】。

## 2. ⚡ 懒加载状态机（强控必做外显） 【规则_2】
- **【阶段 A：初始对齐】（首轮交互绝对硬性强制）**
  1. **【物理读取】** 必须且只能调用工具读取 `activeContext.md` 与 `progress.md`。
  2. **【环境嗅探】** 检查 `.git/` 目录：若存在，运行 `git status` 增量校准进度；若无，切入目标业务文件时动态对齐。
  3. **【对齐凭证】** 完成上述两步后，必须紧接着输出第二行状态标签：`【当前状态：懒加载初始对齐完成】`。**未输出此凭证前，严禁进行任何后续业务开发。**
- **【阶段 B：条件动态激活】（按需读取策略）**
  - `architecture.md`：仅在涉及跨目录重构、全局数据流变更或复杂 Bug 溯源时读取。
  - `systemPatterns.md`：仅在编写核心逻辑、设计模式定义或实现全新架构模式时读取。
  - `techContext.md`：仅在遇到编译构建错误、依赖冲突或需要升级/安装新依赖时读取。
  - `publishing.md`：仅在用户提出「发布」、打 tag 或处理版本控制时读取。

## 3. 🧠 强制增量同步与物理结算 【规则_3】
- **【物理钩子】** 在任意独立功能修改完毕，或提请阶段性反馈、交付前，必须集中同步：
  1. 必须更新 `activeContext.md`（精确追加本次改动的断点、当前逻辑与后续建议）。
  2. 必须更新 `progress.md`（更新任务勾选状态与完成度）。
  3. 若涉及技术栈或目录结构的客观变更，同步增量更新 `techContext.md` 和 `architecture.md`。
  4. **【永久文件保护】** `publishing.md` 仅在初始化时写入。后续任务**绝对禁止**修改或覆盖。变更须经用户明确授权。
- **【交付限制】** 未增量更新上述记忆文件前，严禁向用户发出交付、结项或提请测试的回复。
- **【二次审计】** 写入记忆文件前必须二次核对【规则_0】，严禁将源码块、本地绝对路径或环境信息写入备份。

## 4. 📄 文档编写规范 【规则_4】
- **【设计文档规范】** 设计与架构文档只描述设计思路和核心决策。**严禁包含具体的业务功能实现代码块**（允许使用类型定义、接口 Interface 声明或抽象伪代码作为说明）。

## 5. 🚀 项目形态自适应发布规范 【规则_5】
- **【架构解耦】** 严禁硬编码固化任何特定项目架构。具体的发布目标、构建产物、版本与 Tag 策略，完全由 `./.memory_bank/publishing.md` 动态承载。
- **【环境凭证声明】** 严禁处理或询问任何 Token、密码等登录凭证（完全遵守【规则_0】）。所有发布动作必须完全依赖本地环境已激活的 SSH Key 或本地已登录的凭证状态。遇鉴权错误必须立即报错中断，提示用户手动处理。
- **【通用发布管线】** 收到“发布”指令后，严格执行流控：读取 `publishing.md` → 检查工作区干净度 → 严格按 `publishing.md` 规定的指令集与顺序执行版本 bump、打包或发布 → 本地生成 commit、tag 并物理 push。