# Cline AI 运行规范 (v5.2-精简高控版)

## 0. 🛑 核心红线：绝对隐私与去标识化 【规则_0】
- **【绝对匿名】** 严禁在任何文件中写入可追溯到宿主用户、开发设备或本地环境的痕迹：
  1. **相对路径**：文件路径必须以项目根目录为基准（如 `./src/`），严禁出现物理绝对路径（如 `/Users/...`, `C:\Users\...`）。
  2. **环境阻断**：严禁记录本地 IP、MAC 地址、OS 版本、Git 用户名/邮箱、明文密钥或凭证。
  3. **剔除签名**：严禁在代码中添加包含时间戳或 AI 标志的头部注释（如 `// @author Cline`, `// 修改于...`）。
- **【业务注释】** 严禁以隐私审查为由减少代码注释。复杂业务逻辑、算法分支、API 接口必须编写高水准的纯业务功能注释（遵守 JSDoc、PHPDoc 或 Doxygen 规范）。不含路径和人名的注释均为安全。

## 1. 🔄 强控冷启动与格式死锁 【规则_1】
- **【首行死锁】** 任何全新会话（Session）收到首个任务时，**首行回复**必须强制输出以下状态标签，严禁在此标签前生成任何业务代码或解释：

  * **分支 A：若 `./.memory_bank/` 不存在**
    1. 首行必须且只能回复：`【当前状态：记忆银行未找到 - 正在初始化】`
    2. **【动作中断】** 立即挂起所有业务开发，严禁修改或创建业务文件。
    3. **【环境扫描】** 自动创建 `.memory_bank/` 目录，读取根目录指纹文件（`package.json`/`composer.json`/`CMakeLists.txt`/`README.md`）分析技术栈与目录树。
    4. **【档案初始化】** 立即在 `./.memory_bank/` 下创建以下 5 个**纯英文文件名**的文件，并初始化内容，文件**内部内容必须使用纯中文**书写：
      - `techContext.md`
      - `architecture.md`
      - `systemPatterns.md`
      - `activeContext.md`
      - `progress.md`
    5. 初始化完成后回复：`【当前状态：初始化成功】隐私冷启动分析已完成。现在开始处理您的业务需求...`

  * **分支 B：若 `./.memory_bank/` 已存在**
    1. 首行必须且只能回复：`【当前状态：记忆银行已对齐】`
    2. 立即进入 【规则_2】。

## 2. ⚡ 懒加载状态机（强控必做外显） 【规则_2】
- **【阶段 A：初始对齐】（每轮会话首轮交互必做，绝对硬性强制）**
  1. **【物理读取】** 必须且只能调用工具读取 `activeContext.md` 与 `progress.md`。
  2. **【环境嗅探】** 检查 `.git/` 目录：若存在，运行 `git status` 自动增量校准进度；若无 Git，在切入目标业务文件时动态对齐。
  3. **【对齐凭证】** 完成上述两步后，必须在回复中紧接着输出第二行状态标签：`【当前状态：懒加载初始对齐完成】`。**未输出此对齐凭证前，严禁进行任何后续业务开发。**
- **【阶段 B：条件动态激活】（按需读取策略）**
  - `architecture.md`：仅在涉及跨目录重构或复杂 Bug 溯源时读取。
  - `systemPatterns.md`：仅在编写核心逻辑或实现全新架构模式时读取。
  - `techContext.md`：仅在遇到编译构建错误或升级/安装新依赖时读取。

## 3. 🧠 强制增量同步与物理结算 【规则_3】
- **【物理钩子】** 任何时候只要调用 `write_to_file` 或 `replace_in_file` 修改了任何业务代码，必须在同一批次或紧接着的下一个动作中立刻同步执行：
  1. 必须更新 `activeContext.md`（精确追加本次改动的断点、逻辑与后续建议）。
  2. 必须更新 `progress.md`（更新任务勾选状态与完成度）。
  3. 若涉及技术栈或目录结构的客观变更，同步增量更新 `techContext.md` 和 `architecture.md`。
- **【交付限制】** 未增量更新上述记忆文件前，严禁向用户发出交付、结项或提请测试的文本回复。
- **【二次审计】** 写入记忆文件前必须二次核对【规则_0】，严禁将业务源码块、本地绝对路径或个人环境信息作为历史备份写入其中。

## 4. 📄 文档编写规范 【规则_4】
- **【设计文档禁码】** 设计文档只描述设计思路、规则、策略和决策，严禁出现任何代码实现。